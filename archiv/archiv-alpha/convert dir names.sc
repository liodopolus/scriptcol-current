#!/bin/sh
# delete spaces and replaces caracters from directories

current_directory=$( pwd )

# convert spaces
#for j in * ; do mv "$j" "`echo $j | tr ' ' '-'`" ; done

# insert spaces
#for j in * ; do mv "$j" "`echo $j | tr '-' ' '`" ; done

# replace caracters
#for j in * ; do mv "$j" "`echo $j | sed s/--/-/`" ; done



